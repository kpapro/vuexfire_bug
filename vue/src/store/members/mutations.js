export function setAddMemberDialog (state, addMemberDialog) {
  state.addMemberDialog = addMemberDialog
}

export const setFilterMemberList = function (state, availableMembers) {
  state.availableMembers = availableMembers
}
